{
  "name": "spinning-prize-wheel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:caimi": "vite --mode caimi",
    "build": "bun run build:web",
    "build:web": "vite build",
    "build:web:caimi": "vite build --mode caimi",
    "build:windows": "bun run tauri:build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "bun test",
    "tauri:dev": "tauri dev",
    "tauri:dev:caimi": "tauri dev --config src-tauri/tauri.caimi.conf.json",
    "tauri:build": "bun run tauri:build:caimi && bun run prepare:windows:bundle && bun run tauri:build:installer",
    "tauri:build:caimi": "tauri build --no-bundle --config src-tauri/tauri.caimi.conf.json",
    "tauri:build:installer": "tauri build --config src-tauri/tauri.bundle.conf.json",
    "prepare:windows:bundle": "bun run scripts/prepare-windows-bundle.ts",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tauri-apps/cli": "^2.11.4",
    "@types/bun": "^1.3.14",
    "@types/three": "^0.185.1",
    "svelte": "^5.56.6",
    "svelte-check": "^4.7.3",
    "typescript": "^5.9.0",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "three": "^0.185.1"
  }
}
