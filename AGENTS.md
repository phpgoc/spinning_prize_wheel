# 转盘

## 目标
- 转盘,几种动画可选,简单,奢华,3d
- 动画时间可配置
- 是否有重来可选,这是一个特殊选项,批量和统计时都要考虑,比如有重来时,统计时要把重来的次数也统计进去,批量时也要考虑重来的情况
- 2种模式,选中和俄罗斯轮盘
- 批量模式,可以批量抽奖,比如抽奖100次,统计每个奖项的次数,也可以批量抽奖100次,每次抽到的结果都记录下来
- 纯前端,不依赖服务器
- 编译目标只有web和windows桌面
- 要有个魔法的文本区,粘贴空格,逗号,从excel里粘贴出来的列,都可以正确的解析成选项
- 桌面端可以增加本地数据库,只考虑windows,容易打包进tauri的
- 大量键盘快捷键方便操作,应该是组合键,放置误操作

## 技术栈
- svelte
- tauri
- bun

## 代码规范
- 注释用中文
- 提交用中文

## 现在的问题和修改计划

- 实验室里有个俄罗斯的批量抽取结果,运行选中模拟,运行俄罗斯模拟
- 大富翁不支持俄罗斯,切换的时候让逻辑正常点,现在在转盘大富翁时,切换俄罗斯,还是在大富翁模式,自动给切换到高级就行了 ,切换到俄罗斯后也不要显示大富翁模式
- 网页版的快捷键尽量和tauri一致,如果实在web上有冲突再改.做e2e测试,看看和chrome等有多少冲突,让所有快捷键运转正确.
- 导出excel要引入库吗?如果需要引入超过1M的库,就不引入了,导出excel功能就不做了
- 导出和导入, 能excel就excel,不能就csv,后续都用excel这个词代替
- json需要格式化的,不要一行的,存数据库可以是压缩的,导出的都是格式化的,方便查看
- 排阵分组结果导出excel和json
- 转盘统计导出excel和json
- tauri 历史查询结果导出excel和json
- 排阵点击历史的一次排阵格式化显示
- 排阵导入excel或json
- tauri 历史,界面增加一个打开sqlite 数据文件所在文件夹的功能 抽奖和排阵的历史都可以打开
- 保存可选项里不要保存权重信息,只需要保存选项信息
- localstorage只保存设置,和常用候选人.不要把候选人列表保存,也不要读localstorage
- 大富翁模式,如果当前值和下一个值相同,就不转了,没有移动动画,这是bug,修复下


## 快捷键
- tauri和web的快捷键尽量一致,桌面专用功能除外
- tauri不会有快捷键冲突吧?所以快捷键就非常简单就可以
- web留的浏览器不要和chrome冲突就可以,别的软件就没法考虑了,
- 下边的只是需要带这个键,能简单就尽量简单,考虑tauri,浏览器,输入框
- 快捷键显示的提示,要区分tauri和web区分,显示的不同

### 全局
- 打开从文本区导入 w
- 导出抽奖统计 json e
- 开始新的抽奖,清空候选区 r
- 打开常用选择 a
- 打开快捷键 z
- 开始抽奖 空格
- 切换设置里的保存功能,是否存入数据库 web上没有这个功能tauri专用 s
- 进入候选框选择 x

### 候选项
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 权重增加 alt + 上箭头
- 权重减少 alt + 下箭头
- 删除当前候选项 d
- 取消选中当前选择框 空格
- 编辑当前选择框文字 回车


### 常用选择
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 引入当前选择框的候选项 f
- 退出候选项框 q

### 其他
- 设置,历史,实验室,不需要快捷键,因为不常用,也不需要频繁切换
