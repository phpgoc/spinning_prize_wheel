# 转盘工具

## 目标
- 转盘工具,几种动画可选,简单,奢华,3d
- 动画时间可配置
- 是否有重来可选,这是一个特殊选项,批量和统计时都要考虑,比如有重来时,统计时要把重来的次数也统计进去,批量时也要考虑重来的情况
- 2种模式,选中和俄罗斯轮盘
- 批量模式,可以批量抽奖,比如抽奖100次,统计每个奖项的次数,也可以批量抽奖100次,每次抽到的结果都记录下来
- 纯前端,不依赖服务器
- 编译目标只有web和windows桌面
- 要有个魔法的文本区,粘贴空格,逗号,从excel里粘贴出来的列,都可以正确的解析成选项
- 桌面端可以增加本地数据库,只考虑windows,容易打包进tauri的
- 大量键盘快捷键方便操作,应该是组合键,放置误操作

## 技术栈
- svelte
- tauri
- bun

## 代码规范
- 注释用中文
- 提交用中文

## 现在的问题和修改计划
- 奢华 一点都不奢华,再改的好看点,名字也改的低调点吧
- 3d太丑了,不是这样的,动画也不行,three js就这水平吗?
- 连续抽奖,是在当前统计区最上边设置次数,也是在当前统计区按键,连续到结束 ,要记得手动也算计算次数,到结束也是不是直接结束,也是要按照设置时间规则来操作的
- 猜蜜版, 其实整个项目就相当于4个页面了基础逻辑几乎一样.抽签页名字里有"猜" 有"本"的,中奖概率加倍,俄罗斯被抽到的概率减半.
- 猜蜜版的排阵,在抽签结果后,把自己和同档的一个人交换(如果已经满足条件了可以不交换),找所有组里所任rank分最多,也就是最弱的组,和同档的人交换,被交换的人在table里显示的的明显点,虽然是作弊,就是搞笑的,明作弊
- 猜蜜版的文字要明显点,就是恶搞的,比如"不要被发现了这是猜蜜版本,偷偷",这个意思,词语你自己组织下
- tauri要导出两个可执行文件,普通版本和猜谜版,设置可以是一样的,用localstorage,数据库可以用不同的,或者有个表示位
- 能在网上找到星际猜猜的头像吗,做猜谜版的图标和网页里的icon.
- tauri 要实现本地文件夹里保存数据库和配置. uninstall是要有选择,删不删这些文件
- 打开文本导入有bug,右边在当前统计时,打不开了.当然,已经开始了,目标结果不是0,也不允许再添加了
- 设置里有个字体,默认是多少,可以调大3倍,右边两个折叠页现在是悬浮的,直接把右边占满
- 奖励金额,如果已经转过了,就不能修改了,在tauri,统计打开时
- 设置里有个开关统计,是否存入数据库的,打开时,开启新抽奖时,过去的抽奖结果就入库保存了.
- 打开统计时,后项里的开关选项,就要看现在的统计项是否有内容了,有内容就要清空并入库保存了
- css再斟酌下,界面大气点
- 排阵预览的位置,根据下边的n,做个明显的区分,比如6组一行6个人放不下,也无所谓,但是第6和第7个人要换行,换行后,第7个人的上边要有个明显的分隔线,和第6个人的下边有个明显的分隔线,这个分隔线是横向的.
- 预览那里可以手动添加,选择一个位置,在这中间插入

## 快捷键
- tauri的快捷键和web里的是不一样的
- tauri不会有快捷键冲突吧?所以快捷键就非常简单就可以
- web留的浏览器不要和chrome冲突就可以,别的软件就没法考虑了,
- 下边的只是需要带这个键,能简单就尽量简单,考虑tauri,浏览器,输入框
- 快捷键显示的提示,要区分tauri和web区分,显示的不同

### 全局
- 全局不太容易是单键,都得组合,容易和别的输入输出冲突
- 打开从文本区导入 w
- 导出抽奖统计 json e
- 开始新的抽奖,清空候选区 r
- 打开常用选择 a
- 打开快捷键 z
- 开始抽奖 空格
- 切换设置里的保存功能,是否存入数据库 web上没有这个功能tauri专用 s

### 候选项
- 进入候选框选择 x
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 权重增加 alt + 上箭头
- 权重减少 alt + 下箭头
- 退出候选项框 q
- 删除当前候选项 d
- 取消选中当前选择框 空格
- 编辑当前选择框文字 回车
- 选择到奖励金额 x

### 常用选择
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 引入当前选择框的候选项 f
- 退出候选项框 q


### 其他
- 设置,历史,实验室,不需要快捷键,因为不常用,也不需要频繁切换
