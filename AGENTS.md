# 转盘

## 技术栈
- svelte
- tauri
- bun

## 代码规范
- 注释用中文
- 提交用中文

## 问题和修改
- w 统一为打开文本区域 ,把快捷键描述从转盘,转移到通用,为分组的文本区添加对应功能
- 增加通用的快捷键 h 全屏 ,页面跳转也不会退出全屏吧? 返回也是同一个快捷键,对战区与的区域全屏f,不受此影响
- 历史里每行的文字过小,加深加粗
- 分组和对战在预览和设置区有个名字的选择,抽签时可以为空,双向绑定显示在分组结果和对战区
- 03 对战 8 项 · 同组不对战1对2 · 输入顺序 这里的03是什么意思,这段全不要,如果设置了名字,这里换成名字,如果没显示,显示对战
- 03 分组结果 8 项 · 4 组 · 2 档 · 输入顺序 ,这里的03又是什么意思? 如果设置了名字,这里换成名字,如果没设置显示分组结果
- 如果设置了名字要保存到这两个的历史表里,excel导出的头,如果设置了名字,要以这个名字为主题生成,excel的名字也可以用日期-名字组合
- 分组历史和对战历史中的每一条,如果有名字,要以名字为主题,否则用现有的人数加赛制
- 可直接修正名字；桌面端会核对别名表  删除
- 历史的共n条 改成 a/b条 ,a是规定上限和b的 最小值,b是查询条件下数据库的条数 .比如规定最多5条,数据库里有20条.显示 5/20条 0条显示0/0条 b小于5 都是b/b条 共字去掉,转盘的汇总肯定是针对b的
- 抽奖显示上限为5,对战8,分组 5
- 历史里显示为时间升序,最新的再最下边.查询肯定是desc的,拿出来再颠倒?3个历史都是
- 转盘页的历史改名 转盘历史 里边的时间选择器上边的"历史" 两字删掉
- 请先在右侧粘贴名单 删掉
- 转盘 textarea 里的placeholder 也写每行一个，也支持空格、逗号和 Excel 粘贴 .上边的 粘贴选项文本 删掉
- 设置里的停留时间删掉,统一使用统计里停留时间,并持久化
- 现在使用localstorage保存的,全都改用数据库.key value使用一个表就可以了.为了在桌面版,只拷贝一个sqlite文件就可以跨机器使用了.
- 猜蜜版和非猜本版共享数据库吧.各种历史都不单独区分了.互相也可以看到对方的历史. 只需要一个数据库
- sqlite 桌面版和 web版都要改到.
- 对战区的颜色选择器,里边支付6个16进制的文字输入
- 对战区的hjku移动快捷键已经崩盘,h被全屏占领了,而且h确实会全屏.崩盘,对战区用了wsfl,能用来移动的按键不多了.上下左右被用来内部移动了. 使用gbvn吧,用来微调可能的折叠.
- 对战区选择选择一个比分位后,按esc会选择整个比分框.这个上下依然被这个比分选择框控制.这也没什么,再按下esc可以选择整个对战框,这个时候应该可以用ctrl 上下来修改字体.现在不用鼠标是不可能修改字体了
- 快捷键的 描述非常冗余 上下的描述不需要,回车和esc可以做多种的事,每个写在一行上就可以了

## 修改结果

- [已完成] 恢复键盘排序流程中的数字落点跳转：按空格拿起排名项后，可用数字键定位排名落点，再按空格放下；鼠标拖拽进行中不处理数字跳转。提交：`7e50fcc`。
- [已完成] 删除设置页重复的“停留时间”滑块，统一使用统计区的“结果停留”，并兼容读取旧版配置。提交：`4ee1dcd`。
- [已完成] 历史列表数量改为“a/b条”格式，并移除转盘历史筛选区重复标题。提交：`dbca563`。
- [已完成] 将 W 统一为打开文本区域，并同步更新快捷键说明；分组页和对战页均可聚焦名单文本区。提交：`3347207`。
- [已完成] 增加通用 H 应用全屏快捷键，页面切换后保持全屏；对战区原有 F 局部全屏不受影响。提交：`7003e49`。
- [已完成] 删除分组预览空状态中的“请先在右侧粘贴名单”提示，避免与文本区说明重复。提交：`48bba50`。
- [已完成] 分组和对战设置增加可选名称，结果区和历史列表优先显示名称；历史保存、JSON/Excel 导出使用名称作为主题。提交：`777b1e8`。
- [已完成] 对战颜色控件增加可直接编辑的 6 位十六进制文字输入，并与色盘同步。提交：`8bcbcac`。
- [已完成] 将十六进制输入移入颜色弹出层，外部只保留紧凑的色盘入口。提交：`843eb2f`。
- [已完成] 颜色输入框改为只填写 6 位十六进制数字，组件自动补齐 `#`。提交：`849e1fd`。
- [已完成] 应用设置、转盘设置和对战配色写入 SQLite `app_kv`，桌面版与 Web 版共用同一套配置数据库，并保留旧版设置迁移兼容。提交：`521c7ff`。
- [已完成] 普通版与猜蜜版共享抽奖、分组、对战历史；列表、导入、删除、清空在桌面版与 Web 版均使用同一历史库。提交：`521c7ff`。
- [已完成] 对战历史 JSON 导入时保留历史名称，并补充名称回读测试。提交：`521c7ff`。
- [已完成] 历史行文字增大并加深加粗，提升历史列表可读性。提交：`ac52120`。
- [已完成] 名单预览支持直接修正名称，桌面版继续通过别名表核对并支持别名维护；移除重复说明文字。提交：`ac52120`。
- [已完成] 抽奖、分组、对战历史分别最多展示 5、5、8 条；查询结果仍按数据库倒序统计，列表取最新记录后按时间正序展示，最新记录在最下方。提交：`ac52120`。
- [已完成] 转盘历史标题改为“转盘历史”，文本导入框改用完整说明 placeholder 并移除“粘贴选项文本”标题。提交：`ac52120`。
- [已完成] 对战区微调快捷键改为 G/B（上/下）、V/N（左/右）；比分输入框 Esc 逐级聚焦对战框和整个对战区，整区聚焦时 Ctrl+↑/↓ 可调整字号。提交：`ac52120`。
- [已完成] 精简快捷键说明，移除上下方向键的冗余说明，并合并回车、Esc 的多种用途。提交：`ac52120`。
- [已完成] 快捷键分类改名并调整顺序：通用、确认框、转盘页、常用候选区、候选区、统计区、排名区、分组页、对战页、对战区；为全部分类补齐独立颜色。提交：`7bfb542`。
- [已完成] 对战历史区折叠按钮统一为与转盘、分组历史相同的 `+ / −` 风格。提交：`e8eea54`。
- [已完成] 三个历史区的日期筛选统一为纵向控件，空日期显示 `YYYY / MM / DD`，日字段统一为 `DD`。提交：`3ffef88`。
