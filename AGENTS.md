# 转盘工具

## 目标
- 转盘工具,几种动画可选,简单,奢华,3d
- 动画时间可配置
- 是否有重来可选,这是一个特殊选项,批量和统计时都要考虑,比如有重来时,统计时要把重来的次数也统计进去,批量时也要考虑重来的情况
- 2种模式,选中和俄罗斯轮盘
- 批量模式,可以批量抽奖,比如抽奖100次,统计每个奖项的次数,也可以批量抽奖100次,每次抽到的结果都记录下来
- 纯前端,不依赖服务器
- 编译目标只有web和windows桌面
- 要有个魔法的文本区,粘贴空格,逗号,从excel里粘贴出来的列,都可以正确的解析成选项
- 桌面端可以增加本地数据库,只考虑windows,容易打包进tauri的
- 大量键盘快捷键方便操作,应该是组合键,放置误操作

## 技术栈
- svelte
- tauri
- bun

## 代码规范
- 注释用中文
- 提交用中文

## 现在的问题和修改计划
- 猜蜜版的排阵,在抽签结果后,把自己和同档的一个人交换(如果已经满足条件了可以不交换),找所有组里所任rank分最多,也就是最弱的组,和同档的人交换,被交换的人在table里显示的的明显点,虽然是作弊,就是搞笑的,明作弊
- 猜蜜版俄罗斯抽奖里,名称每有一个"猜"或"本",被抽到的权重减半,且表现上不显示.
- tauri要导出两个可执行文件,普通版本和猜蜜版,设置可以是一样的,用localstorage,数据库可以用不同的,或者有个表示位
- 普通版和猜蜜版共用排名和别名,但抽奖历史与排阵历史都必须按版本隔离.
- 能在网上找到星际猜猜的头像吗,做猜蜜版的图标和网页里的icon.
- tauri 要实现本地文件夹里保存数据库和配置. uninstall是要有选择,删不删这些文件
- css再斟酌下,界面大气点


## 快捷键
- tauri的快捷键和web里的是不一样的
- tauri不会有快捷键冲突吧?所以快捷键就非常简单就可以
- web留的浏览器不要和chrome冲突就可以,别的软件就没法考虑了,
- 下边的只是需要带这个键,能简单就尽量简单,考虑tauri,浏览器,输入框
- 快捷键显示的提示,要区分tauri和web区分,显示的不同

### 全局
- 全局不太容易是单键,都得组合,容易和别的输入输出冲突
- 打开从文本区导入 w
- 导出抽奖统计 json e
- 开始新的抽奖,清空候选区 r
- 打开常用选择 a
- 打开快捷键 z
- 开始抽奖 空格
- 切换设置里的保存功能,是否存入数据库 web上没有这个功能tauri专用 s
- 进入候选框选择 x

### 候选项
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 权重增加 alt + 上箭头
- 权重减少 alt + 下箭头
- 删除当前候选项 d
- 取消选中当前选择框 空格
- 编辑当前选择框文字 回车


### 常用选择
- 移动至上一条 上箭头
- 移动至下一条 下箭头
- 引入当前选择框的候选项 f
- 退出候选项框 q

### 其他
- 设置,历史,实验室,不需要快捷键,因为不常用,也不需要频繁切换
