# 转盘使用说明

## 程序版本

- `转盘.exe`：普通版，适合正式抽签、分组和对战。
- `转盘-猜蜜版.exe`：功能相同，但包含明显的恶搞效果。

两个版本共用排名和别名数据，抽奖、分组及对战记录按版本分别保存。

## 输入名单

名单支持每行一个名称，也支持空格、逗号分隔，或直接粘贴 Excel 中的一整列。

输入完成后点击“确认”，也可以按 `Alt + Enter`。确认后会先显示名单预览，可在正式操作前修正名称和顺序。

## 抽奖

1. 在名单栏输入并确认候选项。
2. 选择抽奖效果、模式和动画时间。
3. 点击转盘中央或按空格开始抽奖。
4. 抽奖历史、批量统计和常用名单位于页面两侧。

按 `Z` 可以打开应用内快捷键总表。

## 分组

1. 输入并确认名单。
2. 设置组数、名单顺序和分组方式。
3. 检查预览后开始分组。
4. 桌面版可以按排名分组，并使用名称别名匹配同一个人。
5. 结果可以导出为 Excel 或 JSON。

## 对战

1. 输入并确认至少 4 名选手。
2. 选择单败、双败或“同组不对战 1 对 2”。
3. 选择按输入顺序或按排名，并设置需要固定的位置。
4. 在预览中确认签位后点击“抽签”。
5. 在每场对战两侧输入比分，胜者和后续场次会自动更新。
6. 点击左上角“全屏”可以让签表占满窗口；按 `F` 可以进入或返回全屏。

对战页常用快捷键：

- `A`：打开或关闭排名。
- `Z`：打开或关闭对战状态。
- `X`：聚焦对战区。
- `W`：聚焦名单。
- `F`：进入或返回全屏。
- `I` / `K`：向上或向下移动。
- `J` / `L`：向左或向右移动。

桌面版会持续保存当前对战临时状态。软件重新启动后，会自动恢复尚未清空的签表和比分。

## 数据和导出

- 桌面版数据保存在当前 Windows 用户的应用数据目录，不会上传到服务器。
- Excel 和 JSON 导出文件默认保存在系统“下载”目录，可以通过页面中的“打开下载”直接查看。
- 卸载时会询问是否删除本地数据。选择“否”可在重新安装后继续使用；选择“是”会删除普通版和猜蜜版的本地数据。
