# 转盘使用说明

## 程序版本

- `转盘.exe`：普通版，适合正式抽签、分组和对战。
- `转盘-猜蜜版.exe`：功能相同，但包含明显的恶搞效果。

两个版本共用排名和别名数据，抽奖、分组及对战记录按版本分别保存。

## 一次完整的使用流程

1. 在顶部选择普通版或猜蜜版。
2. 在名单栏输入名称。每行一个名称，也可以粘贴空格、逗号分隔的文本或 Excel 单列。
3. 点击“确认”生成名单检查区，在这里修正名称、顺序和别名；确认无误后再开始抽奖、分组或对战。
4. 操作过程中可以随时切换字号和界面风格，设置会自动保存在当前版本。

输入框的常用确认方式是 `Alt + Enter`；其他快捷键可在应用内帮助中查看。

## 抽奖

1. 确认候选名单，按需要调整权重、抽奖模式和动画效果。
2. 点击转盘中央开始抽奖；结果、历史和统计会分别显示在对应区域。
3. 想重新开始时，先按页面提示选择是否保留名单和设置，再清空当前结果。
4. 统计结果可导出为 Excel 或 JSON；桌面版可以直接打开下载目录。

## 分组

1. 确认名单后设置组数、分组方式和输入顺序或排名顺序。
2. 先检查分组结果，再执行需要的调整；桌面版可使用排名和别名匹配同一位选手。
3. 完成的分组结果可以保存到历史，也可以导出为 Excel 或 JSON。历史记录按日期筛选，查看时不会改变当前名单。

## 对战

1. 确认至少 4 名选手，选择单败、双败或“同组不对战 1 对 2”。
2. 选择按输入顺序或按排名，并设置前几位固定；签位确认后点击“抽签”。
3. 在对战区域录入每场比分，胜者和后续场次会自动传递；全屏按钮可让签表占满窗口。
4. 任意进行中的状态，以及完全打完的比赛，都可以点击“保存历史”。历史区的记录是只读查看，继续处理时回到下方对战区域。
5. 桌面版会保存未清空的临时对战状态，重新启动后可以继续；历史保存是单独的手动操作。

## 常用设置和数据

- `Ctrl + 上/下` 调整全局字号；字号会同步影响输入框、按钮、文字和签表间距。
- 调色板旁的“保存配色”和“加载配色”使用浏览器本地存储，普通版和猜蜜版分别保存。
- Excel 和 JSON 默认保存到系统“下载”目录。网页版没有系统目录权限，因此不显示“打开下载”。

## 数据和导出

- 桌面版数据保存在当前 Windows 用户的应用数据目录，不会上传到服务器。
- Excel 和 JSON 导出文件默认保存在系统“下载”目录，可以通过页面中的“打开下载”直接查看。
- 卸载时会询问是否删除本地数据。选择“否”可在重新安装后继续使用；选择“是”会删除普通版和猜蜜版的本地数据。
