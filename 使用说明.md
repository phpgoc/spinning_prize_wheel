# 转盘使用说明

## 版本与数据

- `转盘.exe`：普通版，适合正式抽签、分组和对战。
- `转盘-猜蜜版.exe`：功能相同，增加恶搞效果。
- 两个版本共用排名、别名和全部历史记录；进行中的对战临时状态仍按版本分别保存。

## 对战

1. **同组不对战 1 对 2**：仅支持 8 人。名单每两人为一组，组内标记为 1、2。
2. **按排名抽签**：只固定前 N 名，其余选手随机；预览中的其余顺序不代表最终顺序。
3. 在对战区输入比分。胜者和后续签位会自动传递；同一列输入一方比分后，后续场次只需输入败者比分，胜者比分会自动补全。
4. 进行中的对战和已完成的对战都可以点击“保存历史”。历史记录只读；继续处理请回到当前对战区。
5. 要修改历史记录，先将其加载到当前对战区，修改后再手动保存。临时状态不等于历史记录。
6. 分组历史和对战历史都不会自动保存，需手动点击“保存历史”。
7. 未清空的当前对战会自动保存。桌面版重启、网页版刷新后都可以继续；保存历史仍需手动操作。

## 常用设置与数据

- `Ctrl + 上/下`：调整全局字号，输入框、按钮和签表会同步变化。
- “保存配色”和“加载配色”使用本地存储，不会上传数据。
- 桌面版的 Excel、JSON 导出文件默认保存在系统“下载”目录；网页版不提供“打开下载”。

## 数据与导出

- 桌面版数据保存在当前 Windows 用户的应用数据目录，不会上传。
- 网页版数据保存在当前浏览器的 IndexedDB 中，不会上传。
- 卸载时选择“否”可保留本地数据，选择“是”会删除普通版和猜蜜版的数据。
