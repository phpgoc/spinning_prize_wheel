[package]
name = "spinning-prize-wheel"
version = "0.1.0"
description = "A local prize wheel and batch draw laboratory"
authors = ["phpgoc"]
edition = "2021"

[lib]
name = "spinning_prize_wheel_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

